import {login, logout} from '@/api/user.js'
import storage from '@/util/storage.js'

// 注意：vuex的数据是响应式的，但是浏览器一刷新数据会没
const user = {
    state:{
        
    },
    getters:{
        
    },
    mutations:{
        
    },
    // 异步请求放在actions中
    actions:{ 
    }
}

export default user